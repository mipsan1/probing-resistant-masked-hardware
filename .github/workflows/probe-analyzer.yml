# =====================================================================
# probe-analyzer.yml (DISABLED — trace format mismatch)
# =====================================================================
# This workflow is currently disabled because probe/probe_analyzer.py
# expects a bit-level DFF Q wire trace (one line = N ASCII '0'/'1'
# chars, N = 27 for 1st-order, 53 for 2nd-order), but the committed
# sim/power_gl_trace.txt is a Hamming-Distance power trace
# ("cycle_id power_value\n" format) used by probe/tvla.py.
#
# To re-enable this workflow, the bit-level trace must be generated
# from a VCD dump (typically via Icarus Verilog or Verilator) and
# committed as e.g. sim/power_gl_bits.txt, then probe_analyzer.py
# must be invoked with that file.
#
# Until that data pipeline is set up, this workflow is set to
# `if: false` so it does not consume CI minutes.
# =====================================================================
name: Probe analyzer (PyPROBE) — DISABLED (data pipeline pending)

on:
  workflow_dispatch:        # manual run still possible
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  probe-analyzer:
    if: false                 # ← disabled
    name: PyPROBE robust d-probing verification [DISABLED]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: (disabled — see file header for re-enable instructions)
        run: echo "This workflow is disabled. Edit .github/workflows/probe-analyzer.yml and remove 'if: false' to re-enable."
